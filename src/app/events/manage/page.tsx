import ManageClient from "./ManageClient";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Manage events — Gaming Dojo",
};

export default function ManagePage() {
  return <ManageClient />;
}
