import SettingsClient from "./SettingsClient";

export const metadata = {
  title: "Slideshow settings — Gaming Dojo Events",
};

export default function SettingsPage() {
  return <SettingsClient />;
}
