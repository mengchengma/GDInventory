import MembersClient from "./MembersClient";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Register member · Gaming Dojo",
};

export default function MembersPage() {
  return <MembersClient />;
}
